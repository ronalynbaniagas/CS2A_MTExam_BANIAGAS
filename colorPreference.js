// colorPreferences.js

let colorPreferences = [];

for (let i = 0; i < 3; i++) {
    let color = ("Enter a color you like:");
    colorPreferences.push(color);
    console.log("Updated color preferences:", colorPreferences);
}